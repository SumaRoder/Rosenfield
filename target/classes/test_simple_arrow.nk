pub getOk(): R<Int, Str> {
    return Ok(42)
}

pub main(): Int {
    print("Before arrow")
    
    // 最简单的箭头匹配赋值
    result: R<Int, Str> = getOk()
    x: Int = result -> {
        Ok = 100
        Err = 0
    }
    
    print("x = " + x)
    return 0
}